version: "2"

run:
  timeout: 5m
  go: "1.25"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unconvert
    - unused
  disable:
    - depguard

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
